# 软件设计师复习App - 部署指南

## 方案一：Vercel 一键部署（推荐）

### 步骤

1. **初始化 Git 仓库**
```bash
cd d:\work\学习\自我学习\study
git init
git add .
git commit -m "feat: 软件设计师复习App初始版本"
```

2. **推送到 GitHub**
   - 在 GitHub 上创建新仓库 `soft-designer-review`
   - 推送代码到 GitHub

3. **Vercel 部署前端**
   - 登录 https://vercel.com
   - 点击 "New Project"
   - 导入 GitHub 仓库
   - Root Directory 设置为 `frontend`
   - Framework Preset: Vite
   - 点击 Deploy

4. **Vercel 部署后端**
   - 在 Vercel 中创建新项目
   - Root Directory 设置为 `backend`
   - Build Command: `npm run build`
   - Output Directory: `dist`
   - Install Command: `npm install`
   - 环境变量: `PORT=3001`

### 之后每次更新
```bash
git add .
git commit -m "update: 更新内容"
git push
```
Vercel 会自动检测并重新部署。

---

## 方案二：BytePlus Edge Pages 部署（CDN加速）

前端构建后部署到 BytePlus Edge Pages：

```bash
cd frontend
npm run build
# 使用 BytePlus Pages 部署 dist 目录
```

---

## 方案三：本地运行

### 后端
```bash
cd backend
npm install
npm run dev
# 服务启动在 http://localhost:3001
```

### 前端
```bash
cd frontend
npm install
npm run dev
# 开发服务器启动在 http://localhost:5173
```

---

## 环境变量说明

| 变量 | 说明 | 默认值 |
|------|------|--------|
| PORT | 后端端口 | 3001 |
| JWT_SECRET | JWT密钥 | soft-designer-secret-key |
| FRONTEND_URL | 前端地址 | http://localhost:5173 |
